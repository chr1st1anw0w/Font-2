# 動態紋理生成工具 - 控制面板框架說明

**文件作用**: 以文字形式詳細說明所有參數控制面板的結構、配置和交互流程

**版本**: 1.0 MVP
**日期**: 2025-11-10

---

## 📐 整體架構圖

```
┌─────────────────────────────────────────────────────┐
│                    App.tsx                          │
│                                                     │
│  ┌──────────────────────────────────────────────┐  │
│  │         TopBar 工具欄                       │  │
│  │  [Logo] [Import] [Export] [Grid] [Texture]  │  │
│  └──────────────────────────────────────────────┘  │
│                       ↓                             │
│  ┌─────────────────┬──────────────────────────┐   │
│  │  Canvas         │                          │   │
│  │  (紋理預覽      │  TextureControlPanel     │   │
│  │   800×800)      │  (右側邊欄)              │   │
│  │                 │  ┌────────────────────┐  │   │
│  │  🎨             │  │ ⚡ 快速調整        │  │   │
│  │                 │  │ [形狀] [數量]      │  │   │
│  │ SVG rendering   │  │ [🎨 色彩][🔢 數量]│  │   │
│  │                 │  ├────────────────────┤  │   │
│  │                 │  │ [旋轉與尺度]▼      │  │   │
│  │                 │  │ [視覺屬性]▼        │  │   │
│  │                 │  │ [色彩設定]▼        │  │   │
│  │                 │  │ [演算法]▼          │  │   │
│  │                 │  ├────────────────────┤  │   │
│  │                 │  │ [✨ 生成紋理]      │  │   │
│  │                 │  └────────────────────┘  │   │
│  └─────────────────┴──────────────────────────┘   │
│                                                     │
│  ┌──────────────────────────────────────────────┐  │
│  │  BottomBar (輔助工具欄)                      │  │
│  └──────────────────────────────────────────────┘  │
│                                                     │
│  ┌─────────────┐              ┌─────────────────┐ │
│  │ TextureToolbar             │ NavigationBar   │ │
│  │ (左下角)                   │ (右下角)        │ │
│  └─────────────┘              └─────────────────┘ │
└─────────────────────────────────────────────────────┘

┌────────────────────────────┐
│  ColorControlPanel (模態)   │  ← 按需渲染
│  isOpen={showColorPanel}   │
└────────────────────────────┘

┌────────────────────────────┐
│  QuantityControlPanel (模態)│  ← 按需渲染
│  isOpen={showQuantityPanel}│
└────────────────────────────┘
```

---

## 📋 面板詳細配置

### 1️⃣ TextureControlPanel（紋理參數控制面板）

**位置**: 應用右側邊欄
**寬度**: 384px (w-96)
**高度**: 全屏幕高度
**背景**: 深灰色 (bg-gray-900)
**邊框**: 左邊框 (border-l border-gray-800)

#### 結構構成

```
┌─ TextureControlPanel ────────────────────────┐
│                                              │
│ ┌── Header ──────────────────────────────┐  │
│ │ 🛡️ 紋理參數          [✕ 關閉]         │  │
│ └─ border-b border-gray-800 ─────────────┘  │
│                                              │
│ ┌── Content (可滾動) ─────────────────────┐  │
│ │                                         │  │
│ │ ┌─ 級別 1：快速訪問 ──────────────────┐ │  │
│ │ │  (始終展開，紫色漸層背景)           │ │  │
│ │ │                                     │ │  │
│ │ │ ⚡ 快速調整                         │ │  │
│ │ │                                     │ │  │
│ │ │ 【形狀類型】                         │ │  │
│ │ │ [cir] [sqr] [tri] [hex] [pnt]      │ │  │
│ │ │                                     │ │  │
│ │ │ 【元素數量】                         │ │  │
│ │ │ 數量    [45]                         │ │  │
│ │ │ |━━━━━━━━━━━━━━━|                   │ │  │
│ │ │ 1      50      100                  │ │  │
│ │ │                                     │ │  │
│ │ │ 【排列方式】                         │ │  │
│ │ │ [gird] [spir] [radi] [rand] [line]  │ │  │
│ │ │                                     │ │  │
│ │ │ 【快速面板】                         │ │  │
│ │ │ [🎨 色彩] [🔢 數量]                 │ │  │
│ │ └─────────────────────────────────────┘ │  │
│ │                                         │  │
│ │ ┌─ 級別 2a：旋轉與尺度 ──────────────┐ │  │
│ │ │ 旋轉與尺度 ▼                        │ │  │
│ │ │                                     │ │  │
│ │ │ (展開時顯示)                         │ │  │
│ │ │ 旋轉角度: 30°                        │ │  │
│ │ │ |━━━━━━━━━━━|                       │ │  │
│ │ │ 尺度: 1.50x                          │ │  │
│ │ │ |━━━━━━━━━|                         │ │  │
│ │ └─────────────────────────────────────┘ │  │
│ │                                         │  │
│ │ ┌─ 級別 2b：視覺屬性 ──────────────────┐ │  │
│ │ │ 視覺屬性 ▼                          │ │  │
│ │ │                                     │ │  │
│ │ │ (展開時顯示)                         │ │  │
│ │ │ 筆畫寬度: 2px                        │ │  │
│ │ │ |━━━|                               │ │  │
│ │ │ 密度: 65%                            │ │  │
│ │ │ |━━━━━━━|                           │ │  │
│ │ │ 透明度: 100%                         │ │  │
│ │ │ |━━━━━━━━━━━|                       │ │  │
│ │ └─────────────────────────────────────┘ │  │
│ │                                         │  │
│ │ ┌─ 級別 2c：色彩設定 ──────────────────┐ │  │
│ │ │ 🎨 色彩設定  [■ #7c3aed] [■ #fff]  │ │  │
│ │ │ ▼                                   │ │  │
│ │ │                                     │ │  │
│ │ │ (展開時顯示)                         │ │  │
│ │ │ 色彩模式: [single] [gradient]        │ │  │
│ │ │           [palette] [random]        │ │  │
│ │ │ 主要色彩: [■] #7c3aed               │ │  │
│ │ │ 次要色彩: [■] #FFFFFF               │ │  │
│ │ │ 背景色:   [■] #0d1117               │ │  │
│ │ └─────────────────────────────────────┘ │  │
│ │                                         │  │
│ │ ┌─ 級別 2d：演算法設定 ────────────────┐ │  │
│ │ │ 演算法設定 ▼                        │ │  │
│ │ │                                     │ │  │
│ │ │ (展開時顯示)                         │ │  │
│ │ │ 演算法: [perlin] [simplex]           │ │  │
│ │ │         [voronoi] [fibonacci]       │ │  │
│ │ │ 頻率: 0.50                           │ │  │
│ │ │ |━━━━━|                             │ │  │
│ │ │ 振幅: 0.30                           │ │  │
│ │ │ |━━━━|                              │ │  │
│ │ └─────────────────────────────────────┘ │  │
│ │                                         │  │
│ └─────────────────────────────────────────┘  │
│                                              │
│ ┌── Footer ──────────────────────────────┐  │
│ │ [✨ 生成紋理]                          │  │
│ └─ border-t border-gray-800 ─────────────┘  │
│                                              │
│ ┌── 模態層 ──────────────────────────────┐  │
│ │ <ColorControlPanel isOpen={...} />    │  │
│ │ <QuantityControlPanel isOpen={...} /> │  │
│ └────────────────────────────────────────┘  │
│                                              │
└──────────────────────────────────────────────┘
```

#### 級別 1：快速訪問區 - 詳細配置

```
┌─ 快速訪問區 ─────────────────────────────┐
│ 樣式: bg-gradient-to-r from-purple-900/20  │
│       to-transparent                       │
│       border border-purple-700/30          │
│       rounded-lg p-4 space-y-4             │
│                                           │
│ ⚡ 快速調整  (標題)                        │
│ 樣式: text-xs font-bold text-purple-300   │
│       uppercase tracking-wide              │
│                                           │
├─ 1. 【形狀類型】───────────────────────  │
│ grid grid-cols-5 gap-1.5                  │
│ [cir] [sqr] [tri] [hex] [pnt]             │
│                                           │
│ 按鈕樣式:                                  │
│ - 未選中: bg-gray-950 border-gray-700    │
│ - 選中:   bg-purple-600/40 border-purple │
│                                           │
├─ 2. 【數量】───────────────────────────  │
│ 標籤: 數量                  (右側) 45      │
│ 滑桿: <input type="range" min=1 max=100   │
│       value={quantity} />                 │
│ 刻度: 1      50      100                  │
│                                           │
├─ 3. 【排列方式】───────────────────────  │
│ grid grid-cols-3 gap-2                    │
│ [grid] [spiral] [radial] [random] [linear]│
│                                           │
├─ 4. 【快速面板按鈕】───────────────────  │
│ grid grid-cols-2 gap-2                    │
│ pt-2 border-t border-purple-600/30        │
│                                           │
│ [🎨 色彩] [🔢 數量]                       │
│                                           │
│ 按鈕樣式:                                  │
│ bg-gradient-to-r from-purple-600          │
│ to-purple-700                             │
│ hover:from-purple-500 hover:to-purple-600 │
│ text-white rounded text-xs font-semibold  │
│ transition-all                            │
│                                           │
└─────────────────────────────────────────┘
```

#### 級別 2a：旋轉與尺度 - 詳細配置

```
┌─ 旋轉與尺度 (可展開) ────────────────────┐
│                                          │
│ [旋轉與尺度] ▼  (點擊展開)               │
│ 樣式: w-full px-4 py-3 bg-gray-800      │
│       hover:bg-gray-700 flex justify-    │
│       between items-center                │
│                                          │
│ 展開時:                                   │
│                                          │
│ ┌─ Content Area ──────────────────────┐ │
│ │ p-4 bg-gray-850 space-y-4            │ │
│ │                                      │ │
│ │ 旋轉角度          [右側] 30°         │ │
│ │ <input type="range" min=0 max=360    │ │
│ │         value={rotation} />          │ │
│ │                                      │ │
│ │ 尺度              [右側] 1.50x       │ │
│ │ <input type="range" min=0.1 max=5    │ │
│ │         step=0.1 value={scale} />    │ │
│ │                                      │ │
│ └──────────────────────────────────────┘ │
│                                          │
└──────────────────────────────────────────┘
```

#### 級別 2b：視覺屬性 - 詳細配置

```
┌─ 視覺屬性 (可展開) ───────────────────────┐
│                                          │
│ [視覺屬性] ▼  (點擊展開)                 │
│ 樣式: w-full px-4 py-3 bg-gray-800      │
│       hover:bg-gray-700 flex justify-    │
│       between items-center                │
│                                          │
│ 展開時:                                   │
│                                          │
│ ┌─ Content Area ──────────────────────┐ │
│ │ p-4 bg-gray-850 space-y-4            │ │
│ │                                      │ │
│ │ 筆畫寬度          [右側] 2px         │ │
│ │ <input type="range" min=1 max=50     │ │
│ │         value={strokeWidth} />       │ │
│ │                                      │ │
│ │ 密度              [右側] 65%         │ │
│ │ <input type="range" min=0 max=100    │ │
│ │         value={density} />           │ │
│ │                                      │ │
│ │ 透明度            [右側] 100%        │ │
│ │ <input type="range" min=0 max=100    │ │
│ │         value={opacity} />           │ │
│ │                                      │ │
│ └──────────────────────────────────────┘ │
│                                          │
└──────────────────────────────────────────┘
```

#### 級別 2c：色彩設定 - 詳細配置

```
┌─ 色彩設定 (可展開) ────────────────────────┐
│                                           │
│ [🎨 色彩設定] [■#7c3aed][■#ffffff] ▼     │
│ 樣式: w-full px-4 py-3 bg-gray-800       │
│       hover:bg-gray-700 flex justify-     │
│       between items-center                 │
│                                           │
│ 展開時:                                    │
│                                           │
│ ┌─ Content Area ────────────────────────┐ │
│ │ p-4 bg-gray-850 space-y-4              │ │
│ │                                        │ │
│ │ 色彩模式                                │ │
│ │ grid grid-cols-2 gap-2                 │ │
│ │ [single] [gradient] [palette] [random] │ │
│ │                                        │ │
│ │ 主要色彩                                │ │
│ │ [■ 色盤] [#7c3aed (HEX輸入框)]         │ │
│ │ flex items-center gap-2                │ │
│ │ <input type="color" />                 │ │
│ │ <input type="text" />                  │ │
│ │                                        │ │
│ │ 次要色彩                                │ │
│ │ [■ 色盤] [#FFFFFF (HEX輸入框)]         │ │
│ │ flex items-center gap-2                │ │
│ │ <input type="color" />                 │ │
│ │ <input type="text" />                  │ │
│ │                                        │ │
│ │ 背景色                                  │ │
│ │ [■ 色盤] [#0d1117 (HEX輸入框)]         │ │
│ │ flex items-center gap-2                │ │
│ │ <input type="color" />                 │ │
│ │ <input type="text" />                  │ │
│ │                                        │ │
│ └────────────────────────────────────────┘ │
│                                           │
└───────────────────────────────────────────┘
```

#### 級別 2d：演算法設定 - 詳細配置

```
┌─ 演算法設定 (可展開) ──────────────────────┐
│                                           │
│ [演算法設定] ▼  (點擊展開)                │
│ 樣式: w-full px-4 py-3 bg-gray-800       │
│       hover:bg-gray-700 flex justify-     │
│       between items-center                 │
│                                           │
│ 展開時:                                    │
│                                           │
│ ┌─ Content Area ────────────────────────┐ │
│ │ p-4 bg-gray-850 space-y-4              │ │
│ │                                        │ │
│ │ 演算法類型                              │ │
│ │ grid grid-cols-2 gap-2                 │ │
│ │ [perlin] [simplex]                     │ │
│ │ [voronoi] [fibonacci] [none]           │ │
│ │                                        │ │
│ │ 頻率              [右側] 0.50          │ │
│ │ <input type="range" min=0.1 max=2     │ │
│ │         step=0.1 value={frequency} /> │ │
│ │                                        │ │
│ │ 振幅              [右側] 0.30          │ │
│ │ <input type="range" min=0 max=1       │ │
│ │         step=0.1 value={amplitude} /> │ │
│ │                                        │ │
│ └────────────────────────────────────────┘ │
│                                           │
└───────────────────────────────────────────┘
```

---

### 2️⃣ ColorControlPanel（色彩控制面板）

**位置**: 固定疊層模態窗口 (z-50)
**打開方式**: 點擊 TextureControlPanel 中的 [🎨 色彩] 按鈕
**背景底層**: 半透明黑色 (bg-black/50)
**最大寬度**: md (448px)
**狀態**: isOpen={showColorPanel}

#### 結構構成

```
┌─ 半透明背景層 ──────────────────────────────────┐
│ fixed inset-0 bg-black/50 z-50                  │
│                                                 │
│ ┌─ ColorControlPanel 模態 ────────────────────┐ │
│ │ bg-gray-900 border border-gray-700 rounded   │ │
│ │ shadow-2xl w-full max-w-md                   │ │
│ │                                              │ │
│ │ ┌─ 標題欄 ────────────────────────────────┐ │ │
│ │ │ flex items-center justify-between       │ │ │
│ │ │ p-4 border-b border-gray-700            │ │ │
│ │ │ bg-gray-950                             │ │ │
│ │ │                                         │ │ │
│ │ │ 🎨 色彩控制           [✕ 關閉]          │ │ │
│ │ └─────────────────────────────────────────┘ │ │
│ │                                              │ │
│ │ ┌─ 內容區 ────────────────────────────────┐ │ │
│ │ │ p-6 space-y-6                          │ │ │
│ │ │                                         │ │ │
│ │ │ 【色彩模式選擇】                        │ │ │
│ │ │ grid grid-cols-3 gap-2                 │ │ │
│ │ │ [🎨 漸層] [💧 單色] [🎲 隨機]           │ │ │
│ │ │                                         │ │ │
│ │ │ 【主要色彩】                            │ │ │
│ │ │ [■] 色盤 選擇器 / HEX: #7c3aed          │ │ │
│ │ │ flex items-center gap-2                │ │ │
│ │ │                                         │ │ │
│ │ │ 【副色彩】                              │ │ │
│ │ │ [■] 色盤 選擇器 / HEX: #FFFFFF          │ │ │
│ │ │ flex items-center gap-2                │ │ │
│ │ │                                         │ │ │
│ │ │ 【背景色】                              │ │ │
│ │ │ [■] 色盤 選擇器 / HEX: #0d1117          │ │ │
│ │ │ flex items-center gap-2                │ │ │
│ │ │                                         │ │ │
│ │ │ 【漸層預覽】                            │ │ │
│ │ │ w-full h-16 rounded                    │ │ │
│ │ │ linear-gradient(90deg, ...)            │ │ │
│ │ │                                         │ │ │
│ │ │ 【預設調色板】                          │ │ │
│ │ │ grid grid-cols-2 gap-2                 │ │ │
│ │ │ [紫色系] [藍青系]                       │ │ │
│ │ │ [綠色系] [暖色系]                       │ │ │
│ │ │ [紅色系] [靛藍系]                       │ │ │
│ │ │ [粉紅系] [灰階系]                       │ │ │
│ │ │                                         │ │ │
│ │ │ 【提示信息】                            │ │ │
│ │ │ p-3 bg-gray-800/30 rounded              │ │ │
│ │ │ 💡 色彩會即時反映在預覽中               │ │ │
│ │ │                                         │ │ │
│ │ └─────────────────────────────────────────┘ │ │
│ │                                              │ │
│ │ ┌─ 底部按鈕欄 ────────────────────────────┐ │ │
│ │ │ border-t border-gray-700 p-4            │ │ │
│ │ │ bg-gray-950 flex gap-2                  │ │ │
│ │ │                                         │ │ │
│ │ │ [確認] [取消]                           │ │ │
│ │ │ flex-1 py-2 rounded                     │ │ │
│ │ │                                         │ │ │
│ │ └─────────────────────────────────────────┘ │ │
│ │                                              │ │
│ └──────────────────────────────────────────────┘ │
│                                                 │
└─────────────────────────────────────────────────┘
```

#### 詳細配置說明

```
【色彩模式選擇】
┌──────────────────────────────────────────┐
│ grid grid-cols-3 gap-2                   │
│                                          │
│ [🎨 漸層]  - 兩色漸層模式                │
│ [💧 單色]  - 單一色彩模式                │
│ [🎲 隨機]  - 隨機色彩變化                │
│                                          │
│ 按鈕樣式:                                 │
│ - 未選中: bg-gray-800 border-gray-700   │
│ - 選中:   bg-purple-600/40 border-purple│
│           text-purple-200 font-semibold │
│                                          │
└──────────────────────────────────────────┘

【主要色彩 & 副色彩 & 背景色 (相同結構)】
┌──────────────────────────────────────────┐
│ 【主要色彩】                              │
│ flex items-center gap-2                  │
│ w-full                                   │
│                                          │
│ [■ 色盤]  [#7c3aed........]              │
│  12px      flex-1 HEX輸入框              │
│                                          │
│ <input type="color"                      │
│         value={primaryColor}             │
│         className="w-12 h-10 rounded     │
│         cursor-pointer border ..." />    │
│                                          │
│ <input type="text"                       │
│         value={primaryColor}             │
│         className="flex-1 bg-gray-950    │
│         border rounded px-2 py-1 text-xs│
│         uppercase font-mono" />          │
│                                          │
└──────────────────────────────────────────┘

【漸層預覽】
┌──────────────────────────────────────────┐
│ w-full h-16 rounded                      │
│ ▲                                        │
│ ├─ linear-gradient(90deg,                │
│ │   from-purple-600,                    │
│ │   to-purple-500)                      │
│ │                                        │
│ └─ CSS style={{                          │
│    background: `linear-gradient(90deg,  │
│    ${primaryColor}, ${secondaryColor})`  │
│    }}                                    │
│                                          │
└──────────────────────────────────────────┘

【預設調色板】
┌──────────────────────────────────────────┐
│ 共 8 組預設調色板:                        │
│                                          │
│ 1. 紫色系    (#7c3aed → #5847eb)        │
│ 2. 藍青系    (#0891b2 → #06b6d4)        │
│ 3. 綠色系    (#059669 → #10b981)        │
│ 4. 暖色系    (#ea580c → #f97316)        │
│ 5. 紅色系    (#dc2626 → #ef4444)        │
│ 6. 靛藍系    (#4f46e5 → #6366f1)        │
│ 7. 粉紅系    (#ec4899 → #f43f5e)        │
│ 8. 灰階系    (#6b7280 → #d1d5db)        │
│                                          │
│ grid grid-cols-2 gap-2                  │
│ 每個按鈕: py-2 px-3 rounded              │
│           bg-gradient-to-r 相應顏色     │
│           text-xs font-semibold          │
│                                          │
└──────────────────────────────────────────┘
```

---

### 3️⃣ QuantityControlPanel（數量控制面板）

**位置**: 固定疊層模態窗口 (z-50)
**打開方式**: 點擊 TextureControlPanel 中的 [🔢 數量] 按鈕
**背景底層**: 半透明黑色 (bg-black/50)
**最大寬度**: md (448px)
**狀態**: isOpen={showQuantityPanel}

#### 結構構成

```
┌─ 半透明背景層 ──────────────────────────────────┐
│ fixed inset-0 bg-black/50 z-50                  │
│                                                 │
│ ┌─ QuantityControlPanel 模態 ───────────────┐  │
│ │ bg-gray-900 border border-gray-700         │  │
│ │ rounded-lg shadow-2xl w-full max-w-md      │  │
│ │                                            │  │
│ │ ┌─ 標題欄 ──────────────────────────────┐ │  │
│ │ │ flex items-center justify-between    │ │  │
│ │ │ p-4 border-b border-gray-700         │ │  │
│ │ │ bg-gray-950                          │ │  │
│ │ │                                      │ │  │
│ │ │ 🔢 數量與排列控制      [✕ 關閉]      │ │  │
│ │ └──────────────────────────────────────┘ │  │
│ │                                            │  │
│ │ ┌─ 內容區 ──────────────────────────────┐ │  │
│ │ │ p-6 space-y-6                        │ │  │
│ │ │                                      │ │  │
│ │ │ 【基本數量】                          │ │  │
│ │ │ 元素數量  [45]                        │ │  │
│ │ │ |━━━━━━━━━━━|                        │ │  │
│ │ │ 1        50        100               │ │  │
│ │ │                                      │ │  │
│ │ │ 【元素密度指示】                      │ │  │
│ │ │ p-3 bg-gray-800/50 rounded           │ │  │
│ │ │ 元素密度: 45%                         │ │  │
│ │ │ |━━━━━━━|                            │ │  │
│ │ │ ♦━━━━━━━━━━━♦ (漸層色塊)            │ │  │
│ │ │                                      │ │  │
│ │ │ 【排列方式】                          │ │  │
│ │ │ grid grid-cols-2 gap-2               │ │  │
│ │ │ [🔲 網格] [🌀 螺旋]                   │ │  │
│ │ │ [⭕ 徑向] [🎲 隨機]                   │ │  │
│ │ │                                      │ │  │
│ │ │ 【網格預設】                          │ │  │
│ │ │ grid grid-cols-3 gap-2               │ │  │
│ │ │ [3×3] [4×4] [5×5]                    │ │  │
│ │ │ [6×6] [8×8]                          │ │  │
│ │ │                                      │ │  │
│ │ │ 自訂值: 5×9 = 45 元素                 │ │  │
│ │ │ text-xs text-gray-500 mt-2            │ │  │
│ │ │                                      │ │  │
│ │ │ 【排列預覽】                          │ │  │
│ │ │ aspect-square bg-gray-800/50         │ │  │
│ │ │ rounded border border-gray-700       │ │  │
│ │ │ p-4                                  │ │  │
│ │ │                                      │ │  │
│ │ │ ┌─ 網格預覽 ───────────────────┐    │ │  │
│ │ │ │ grid display 5×9             │    │ │  │
│ │ │ │ [■][■][■][■][■]              │    │ │  │
│ │ │ │ [■][■][■][■][■]              │    │ │  │
│ │ │ │ [■][■][■][■][■]              │    │ │  │
│ │ │ │ ... (最多顯示 25 個)          │    │ │  │
│ │ │ └──────────────────────────────┘    │ │  │
│ │ │                                      │ │  │
│ │ │ 共 45 個元素 (5×9)                   │ │  │
│ │ │ text-xs text-center mt-2             │ │  │
│ │ │                                      │ │  │
│ │ │ 【提示信息】                          │ │  │
│ │ │ p-3 bg-gray-800/30 rounded           │ │  │
│ │ │ 💡 提示: 增加元素數量會使...         │ │  │
│ │ │                                      │ │  │
│ │ └──────────────────────────────────────┘ │  │
│ │                                            │  │
│ │ ┌─ 底部按鈕欄 ──────────────────────────┐ │  │
│ │ │ border-t border-gray-700 p-4          │ │  │
│ │ │ bg-gray-950                           │ │  │
│ │ │                                        │ │  │
│ │ │ [確認]                                │ │  │
│ │ │ flex-1 py-2 px-4 bg-purple-600        │ │  │
│ │ │ hover:bg-purple-700 rounded           │ │  │
│ │ │ text-white font-medium                │ │  │
│ │ │                                        │ │  │
│ │ └──────────────────────────────────────┘ │  │
│ │                                            │  │
│ └────────────────────────────────────────────┘  │
│                                                 │
└─────────────────────────────────────────────────┘
```

#### 詳細配置說明

```
【基本數量 - 數量滑桿】
┌──────────────────────────────────────────┐
│ 標籤區:                                   │
│ flex items-center justify-between mb-3   │
│ 元素數量          [右側] 45              │
│ text-2xl font-bold text-purple-400       │
│                                          │
│ 滑桿:                                    │
│ <input type="range"                      │
│         min="1" max="100"                │
│         value={quantity}                 │
│         onChange={...}                   │
│         onMouseUp={...}                  │
│         style={{                         │
│           height: '8px',                 │
│           borderRadius: '4px',           │
│           background: '#374151',         │
│         }} />                            │
│                                          │
│ 刻度標簽:                                 │
│ flex justify-between text-xs             │
│ text-gray-500 mt-2                       │
│ 1      50      100                       │
│                                          │
└──────────────────────────────────────────┘

【元素密度指示】
┌──────────────────────────────────────────┐
│ p-3 bg-gray-800/50 rounded                │
│ border border-gray-700                   │
│                                          │
│ 元素密度: 45%                             │
│ text-xs text-gray-400 mb-2               │
│                                          │
│ [進度條]                                  │
│ w-full h-2 bg-gray-700 rounded-full      │
│ overflow-hidden                          │
│                                          │
│ ├─ 內部填充 (45%)                        │
│ │  h-full bg-gradient-to-r               │
│ │  from-purple-500 to-purple-600         │
│ │  transition-all                        │
│ │  width: 45%                            │
│ │                                        │
│ └─────────────────────────────────────┘  │
│                                          │
└──────────────────────────────────────────┘

【排列方式】
┌──────────────────────────────────────────┐
│ grid grid-cols-2 gap-2                   │
│                                          │
│ [🔲 網格]   - Grid 規則排列              │
│ [🌀 螺旋]   - Spiral 螺旋排列            │
│ [⭕ 徑向]   - Radial 放射狀排列          │
│ [🎲 隨機]   - Random 隨機排列            │
│                                          │
│ 按鈕樣式:                                 │
│ py-2 rounded border transition-colors    │
│ text-sm font-medium capitalize           │
│ - 未選中: bg-gray-800 border-gray-700   │
│           text-gray-400 hover:border-... │
│ - 選中:   bg-purple-600/40                │
│           border-purple-500              │
│           text-purple-200                │
│                                          │
└──────────────────────────────────────────┘

【網格預設】
┌──────────────────────────────────────────┐
│ grid grid-cols-3 gap-2                   │
│                                          │
│ [3×3] [4×4] [5×5] [6×6] [8×8]           │
│                                          │
│ 按鈕樣式:                                 │
│ py-2 rounded border transition-colors    │
│ text-sm font-medium                      │
│ - 未選中: bg-gray-800 border-gray-700   │
│ - 選中:   bg-purple-600/40 text-purple   │
│           border-purple-500              │
│                                          │
│ 自訂值顯示:                               │
│ text-xs text-gray-500 mt-2               │
│ 自訂值: 5×9 = 45 元素                     │
│                                          │
└──────────────────────────────────────────┘

【排列預覽】
┌──────────────────────────────────────────┐
│ aspect-square bg-gray-800/50 rounded     │
│ border border-gray-700 p-4               │
│                                          │
│ ┌─ Grid Container ──────────────────┐   │
│ │ w-full h-full bg-gray-900 rounded │   │
│ │ border border-gray-700            │   │
│ │ display: grid                     │   │
│ │ gridTemplateColumns:              │   │
│ │   repeat(5, 1fr)  ← 列數          │   │
│ │ gap: 4px                          │   │
│ │                                   │   │
│ │ [■][■][■][■][■]  - 每個: 紫色     │   │
│ │ [■][■][■][■][■]    bg-purple-    │   │
│ │ [■][■][■][■][■]    600/30        │   │
│ │ [■][■][■][■][■]    border-purple │   │
│ │ [■][■][■][■][■]    500/50        │   │
│ │ (最多顯示 25 個)                   │   │
│ │                                   │   │
│ └───────────────────────────────────┘   │
│                                          │
│ 共 45 個元素 (5×9)                       │
│ text-xs text-gray-500 text-center mt-2  │
│                                          │
└──────────────────────────────────────────┘

【提示信息】
┌──────────────────────────────────────────┐
│ p-3 bg-gray-800/30 rounded                │
│ border border-gray-700/50                │
│                                          │
│ 💡 提示: 增加元素數量會使紋理更密集，    │
│ 減少會使其更疏散。                       │
│                                          │
│ text-xs text-gray-400                    │
│                                          │
└──────────────────────────────────────────┘
```

---

### 4️⃣ NavigationBar（導航欄）

**位置**: 右下角浮動
**背景**: 深灰色 (bg-gray-800)
**邊框**: border border-gray-700
**陰影**: shadow-xl
**位置類**: fixed bottom-6 right-6
**堆疊層**: z-40

#### 結構構成

```
┌─ 右下角浮動導航欄 ─────────────────────────┐
│ fixed bottom-6 right-6                   │
│ bg-gray-800 border border-gray-700       │
│ rounded-lg shadow-xl p-4                 │
│ flex items-center gap-3 z-40             │
│                                          │
│ ┌─ 縮放信息 ─────────────────────────┐  │
│ │ flex items-center gap-3             │  │
│ │                                     │  │
│ │ 【尺寸顯示】                        │  │
│ │ 800 × 800  (canvas 尺寸)           │  │
│ │ text-xs text-gray-400 font-mono     │  │
│ │                                     │  │
│ │ 【分隔線】                          │  │
│ │ w-px h-6 bg-gray-600               │  │
│ │                                     │  │
│ │ 【縮放按鈕組】                      │  │
│ │                                     │  │
│ │ [−] [100%▼] [+]  [1:1] [⊡]         │  │
│ │                                     │  │
│ │ 【縮放百分比選單 (按需顯示)】       │  │
│ │ position: absolute bottom-full      │  │
│ │ mb-2 right-0                        │  │
│ │ bg-gray-900 rounded-lg              │  │
│ │ py-1 min-w-[120px] z-50             │  │
│ │                                     │  │
│ │ [25%]  ← 選項                       │  │
│ │ [50%]                               │  │
│ │ [75%]                               │  │
│ │ [100%] ← 當前選中                   │  │
│ │ [125%]                              │  │
│ │ [150%]                              │  │
│ │ [200%]                              │  │
│ │                                     │  │
│ └─────────────────────────────────────┘  │
│                                          │
└──────────────────────────────────────────┘
```

#### 詳細按鈕配置

```
【縮小按鈕】
┌────────────────────────────────────────┐
│ p-1.5 hover:bg-gray-700 rounded         │
│ transition-colors                       │
│ text-gray-400 hover:text-white          │
│ title="縮小 (Ctrl + -)"                 │
│                                         │
│ SVG: 放大鏡減號 (16×16)                 │
│ <circle cx="11" cy="11" r="8" />        │
│ <path d="m21 21-4.35-4.35" />           │
│ <line x1="8" y1="11" x2="14" y2="11" /> │
│                                         │
└────────────────────────────────────────┘

【縮放百分比顯示】
┌────────────────────────────────────────┐
│ min-w-[70px] px-2 py-1.5                │
│ text-sm font-mono font-semibold         │
│ text-white bg-gray-700 hover:bg-gray-600│
│ rounded transition-colors               │
│ flex items-center justify-between       │
│                                         │
│ 100%        (當前百分比)                │
│              ▼ (下拉箭頭, 12×12)        │
│                                         │
│ onClick={() => toggleZoomMenu()}        │
│                                         │
│ 下拉箭頭轉動:                            │
│ className={`ml-1 transition-transform  │
│   ${showZoomMenu ? 'rotate-180' : ''}`} │
│                                         │
└────────────────────────────────────────┘

【放大按鈕】
┌────────────────────────────────────────┐
│ p-1.5 hover:bg-gray-700 rounded         │
│ transition-colors                       │
│ text-gray-400 hover:text-white          │
│ title="放大 (Ctrl + +)"                 │
│                                         │
│ SVG: 放大鏡加號 (16×16)                 │
│ <circle cx="11" cy="11" r="8" />        │
│ <path d="m21 21-4.35-4.35" />           │
│ <line x1="8" y1="11" x2="14" y2="11" /> │
│ <line x1="11" y1="8" x2="11" y2="14" /> │
│                                         │
└────────────────────────────────────────┘

【重置按鈕 (1:1)】
┌────────────────────────────────────────┐
│ px-3 py-1.5                             │
│ text-xs font-semibold                   │
│ text-gray-300 hover:text-white          │
│ bg-gray-700 hover:bg-gray-600           │
│ rounded transition-colors               │
│ title="重置為 100%"                     │
│                                         │
│ 1:1  (1倍放大率)                        │
│                                         │
└────────────────────────────────────────┘

【適應屏幕按鈕】
┌────────────────────────────────────────┐
│ px-3 py-1.5                             │
│ text-xs font-semibold                   │
│ text-gray-300 hover:text-white          │
│ bg-gray-700 hover:bg-gray-600           │
│ rounded transition-colors               │
│ flex items-center gap-1                 │
│ title="適應屏幕"                        │
│                                         │
│ SVG: 四角邊框 (14×14)                   │
│ <path d="M8 3H5a2 2 0 0 0-2 2v3 ..."/>  │
│ ⊡  (方形符號)                           │
│                                         │
└────────────────────────────────────────┘

【縮放菜單選項】 (按需顯示)
┌────────────────────────────────────────┐
│ absolute bottom-full mb-2 right-0       │
│ bg-gray-900 border border-gray-700      │
│ rounded-lg shadow-xl py-1 min-w-[120px]│
│ z-50                                    │
│                                         │
│ 每個選項:                                │
│ w-full px-4 py-2 text-sm text-left     │
│ hover:bg-gray-800 transition-colors     │
│                                         │
│ 【當前選中樣式】                        │
│ bg-purple-600/30                        │
│ text-purple-200 font-semibold           │
│                                         │
│ 【未選中樣式】                          │
│ text-gray-300                           │
│                                         │
│ 選項列表:                                │
│ [25%] [50%] [75%] [100%]                │
│ [125%] [150%] [200%]                    │
│                                         │
└────────────────────────────────────────┘
```

---

## 🔄 交互流程圖

### 用戶流程 A：快速色彩調整

```
用戶打開應用
    ↓
看到 TextureControlPanel （右側邊欄）
    ↓
點擊 [🎨 色彩] 按鈕
    ↓ (state: showColorPanel = true)
ColorControlPanel 模態顯示
    ├→ 選擇色彩模式 (漸層/單色/隨機)
    ├→ 調整主要色彩 (#7c3aed)
    ├→ 調整副色彩 (#FFFFFF)
    ├→ 或點擊預設調色板
    └→ 實時預覽更新
    ↓
點擊 [確認] 按鈕
    ↓ (state: showColorPanel = false)
ColorControlPanel 關閉
    ↓
TextureControlPanel 刷新色彩預覽
    ↓
用戶可點擊 [✨ 生成紋理] 生成

流程時間: ~ 3-5 秒
```

### 用戶流程 B：調整數量與排列

```
用戶在 TextureControlPanel 中
    ↓
點擊 [🔢 數量] 按鈕
    ↓ (state: showQuantityPanel = true)
QuantityControlPanel 模態顯示
    ├→ 調整數量滑桿 (1-100)
    │   └→ 實時更新密度指示器
    ├→ 選擇排列方式 (網格/螺旋/徑向/隨機)
    ├→ 或點擊網格預設 (3×3, 4×4, ...)
    │   └→ 實時更新網格預覽
    └→ 預覽顯示實際佈局
    ↓
點擊 [確認] 按鈕
    ↓ (state: showQuantityPanel = false)
QuantityControlPanel 關閉
    ↓
TextureControlPanel 刷新數量和排列
    ↓
用戶可點擊 [✨ 生成紋理] 生成

流程時間: ~ 3-5 秒
```

### 用戶流程 C：完整工作流

```
1. 打開應用，看到預設紋理參數
   └→ TextureControlPanel 展開（右側）
   └→ NavigationBar 就位（右下角）

2. 快速訪問區調整：
   ├→ 選擇形狀 (快速選擇)
   └→ 調整數量 (快速滑桿)

3. 點擊 [🎨 色彩] 開啟色彩面板
   ├→ 選擇主副色彩
   ├→ 預覽漸層效果
   └→ 點擊確認返回

4. 點擊 [🔢 數量] 開啟數量面板
   ├→ 微調元素數量
   ├→ 選擇排列方式
   └→ 預覽網格佈局

5. 在詳細設定區展開：
   ├→ 調整旋轉角度
   ├→ 調整視覺屬性 (筆寬、密度、透明度)
   └→ 選擇演算法並微調

6. 點擊 [✨ 生成紋理]
   ├→ Canvas 實時渲染 SVG
   └→ 右下角 NavigationBar 可進行縮放

7. 點擊 TextureToolbar（左下角）匯出
   ├→ [SVG] 向量格式
   ├→ [PNG] 光柵格式
   └→ [CSS] 網頁就緒格式

完整流程時間: ~ 30-60 秒
```

---

## 📊 狀態管理清單

### TextureControlPanel 本地狀態

```typescript
// 展開/折疊狀態
const [expandedSections, setExpandedSections] = useState<Set<string>>(
  new Set(['basic'])  // 初始只展開快速訪問區
);

// 模態顯示狀態
const [showColorPanel, setShowColorPanel] = useState(false);
const [showQuantityPanel, setShowQuantityPanel] = useState(false);

// 受控於父組件 (App.tsx)
interface Props {
  parameters: TextureParameters;  // 完整參數
  onParametersChange: Function;   // 參數更新回調
  onGenerate: Function;           // 生成紋理回調
  isGenerating?: boolean;         // 生成中狀態
}
```

### ColorControlPanel 本地狀態

```typescript
// 色彩模式切換
const [colorMode, setColorMode] = useState<'gradient' | 'single' | 'random'>(
  'gradient'
);

// 所有色彩通過 Props 和回調受控於父組件
interface Props {
  primaryColor: string;           // 主色彩 (#7c3aed)
  secondaryColor: string;         // 副色彩 (#FFFFFF)
  backgroundColor: string;        // 背景色 (#0d1117)
  onPrimaryColorChange: Function;
  onSecondaryColorChange: Function;
  onBackgroundColorChange: Function;
  isOpen: boolean;               // 模態開關
  onClose: Function;             // 關閉回調
}
```

### QuantityControlPanel 本地狀態

```typescript
// 臨時數量 (調整中)
const [tempQuantity, setTempQuantity] = useState(quantity);

// 監聽 Props 更新
useEffect(() => {
  setTempQuantity(quantity);
}, [quantity]);

// 所有數量通過 Props 和回調受控於父組件
interface Props {
  quantity: number;               // 當前元素數量 (1-100)
  arrangement: string;            // 排列方式
  onQuantityChange: Function;
  onArrangementChange: Function;
  isOpen: boolean;               // 模態開關
  onClose: Function;             // 關閉回調
}
```

### App.tsx 全局狀態

```typescript
// 紋理相關狀態
const [textureMode, setTextureMode] = useState(false);
const [textureParameters, setTextureParameters] =
  useState<TextureParameters>(defaultTextureParameters);
const [textureResult, setTextureResult] =
  useState<TextureGenerationResult | null>(null);
const [isGeneratingTexture, setIsGeneratingTexture] = useState(false);
const [showTexturePanel, setShowTexturePanel] = useState(false);

// 縮放狀態
const [zoomLevel, setZoomLevel] = useState(100);
```

---

## 🎨 色彩設計方案

### 主題色彩系統

```
【深色背景】
- 應用背景: #0d1117 (bg-gray-950)
- 面板背景: #1a202c (bg-gray-900)
- 區域背景: #111827 (bg-gray-850)
- 懸停背景: #1f2937 (bg-gray-800)

【邊框與分隔】
- 主邊框: #374151 (border-gray-700)
- 淡邊框: #4b5563 (border-gray-600)
- 淡淡邊框: #6b7280 (border-gray-500)

【文本顏色】
- 主文本: #f3f4f6 (text-gray-100)
- 次文本: #d1d5db (text-gray-300)
- 淡文本: #9ca3af (text-gray-400)
- 極淡: #6b7280 (text-gray-500)

【功能色彩】
- 主色 (紫): #7c3aed (bg-purple-600)
- 深紫: #5847eb (purple-500 or indigo-500)
- 輔色 (靛): #4f46e5 (bg-indigo-600)

【反饋色彩】
- 成功: #10b981 (green-500)
- 警告: #f59e0b (amber-500)
- 危險: #ef4444 (red-500)
```

### 漸層應用

```
【快速訪問區漸層】
background: linear-gradient(
  to right,
  rgba(109, 40, 217, 0.2),  /* purple-900/20 */
  transparent
)

【預設色按鈕漸層】
background: linear-gradient(
  to right,
  #7c3aed,  /* from */
  #5847eb   /* to */
)

【密度指示漸層】
background: linear-gradient(
  to right,
  #a855f7,  /* purple-500 */
  #9333ea   /* purple-600 */
)
```

---

## ⌨️ 鍵盤快捷鍵

### 支持的快捷鍵

```
【縮放控制】
Ctrl + Mouse Wheel Up    → 放大 (+10%)
Ctrl + Mouse Wheel Down  → 縮小 (-10%)
Ctrl + 0                 → 重置到 100%
Ctrl + 1                 → 適應屏幕

【面板控制】
Esc                      → 關閉任何打開的模態

【生成和導出】
Ctrl + Enter             → 生成紋理 (TODO)
Ctrl + S                 → 快速導出 (TODO)
```

---

## 📱 響應式設計

### 不同屏幕尺寸

```
【桌面 (> 1920px)】
┌─ TextureControlPanel ──────────────┐
│ w-96 (384px) | Canvas 1536px       │
│ 所有面板完整展示                   │
│ NavigationBar: 120px (完整)        │
└────────────────────────────────────┘

【平板 (768px - 1920px)】
┌─ TextureControlPanel ──────────────┐
│ w-80 (320px) | Canvas 自適應       │
│ 部分面板需展開                     │
│ NavigationBar: 100px (縮小)        │
└────────────────────────────────────┘

【手機 (< 768px)】
┌────────────────────────────────────┐
│ Canvas 全屏 (底部按鈕)             │
│                                    │
│ TextureControlPanel → 底部抽屜     │
│ (需要額外實現: Drawer 組件)        │
│                                    │
│ NavigationBar → 工具欄底部         │
└────────────────────────────────────┘
```

---

## 🔧 配置項目參考

### DefaultTextureParameters

```typescript
{
  id: 'texture-001',
  name: '默認紋理',

  // 基本配置
  shapeType: 'circle',
  quantity: 45,
  arrangement: 'grid',

  // 旋轉與尺度
  rotationType: 'fixed',
  rotation: 30,
  scale: 1.5,

  // 色彩
  colorMode: 'gradient',
  primaryColor: '#7c3aed',
  secondaryColor: '#5847eb',
  backgroundColor: '#0d1117',

  // 視覺屬性
  strokeWidth: 2,
  opacity: 100,
  density: 65,
  blendMode: 'normal',

  // 演算法
  algorithm: 'perlin',
  algorithmParams: {
    frequency: 0.5,
    amplitude: 0.3,
    octaves: 4,
  },

  // 畫布設定
  canvasWidth: 800,
  canvasHeight: 800,
}
```

---

## 📝 更新日誌

**版本 1.0 - 2025-11-10**
- ✅ 完整框架說明文檔
- ✅ 所有面板詳細配置
- ✅ 交互流程圖
- ✅ 狀態管理清單
- ✅ 色彩設計方案

**下個版本待做**
- ⏳ 響應式優化
- ⏳ 移動端 Drawer 組件
- ⏳ 鍵盤快捷鍵實現
- ⏳ 性能監控

---

**文檔維護**: AI 助手
**最後更新**: 2025-11-10
**狀態**: 完成 ✅

