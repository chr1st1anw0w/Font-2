# 控制面板交互關係圖

**用途**: 直觀展示所有面板之間的通信、狀態流轉和用戶交互路徑

**更新日期**: 2025-11-10

---

## 🏗️ 整體架構圖

```
┌─────────────────────────────────────────────────────────────────┐
│                           App.tsx                               │
│                       (狀態管理層)                              │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  textureParameters (TextureParameters)                  │   │
│  │  ├─ shapeType, quantity, arrangement                   │   │
│  │  ├─ rotation, scale                                    │   │
│  │  ├─ primaryColor, secondaryColor, backgroundColor      │   │
│  │  ├─ strokeWidth, density, opacity                      │   │
│  │  ├─ algorithm, algorithmParams                         │   │
│  │  └─ ... 更多參數                                       │   │
│  │                                                          │   │
│  │  isGeneratingTexture (boolean)                          │   │
│  │  showTexturePanel (boolean)                             │   │
│  │  textureResult (TextureGenerationResult | null)         │   │
│  │  zoomLevel (number: 25-200)                             │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
└────────┬────────────────────────┬──────────────────┬────────────┘
         │                        │                  │
         ▼                        ▼                  ▼
    ┌────────────┐          ┌──────────────┐   ┌──────────────┐
    │  Canvas    │          │  TopBar      │   │  BottomBar   │
    │            │          │              │   │              │
    │  800×800   │          │ [Import]     │   │ [工具欄]     │
    │  SVG →PNG  │          │ [Export]     │   │              │
    │  rendering │          │ [Texture]    │   └──────────────┘
    │            │          └──────────────┘
    └────┬───────┘
         │
         └─ 實時預覽已選參數的紋理效果
```

---

## 🔗 核心通信架構

### 層級 1: 主面板 ↔ 模態面板

```
┌─────────────────────────────────────────────────┐
│            TextureControlPanel                  │
│          (右側邊欄，始終可見)                  │
│                                                 │
│  ╔═══════════════════════════════════════════╗ │
│  ║ 級別 1: 快速訪問區                        ║ │
│  ║ [形狀][數量][排列]                         ║ │
│  ║                                            ║ │
│  ║ ┌──────────────┐  ┌──────────────┐        ║ │
│  ║ │ [🎨 色彩]    │  │ [🔢 數量]    │        ║ │
│  ║ │ 點擊觸發     │  │ 點擊觸發     │        ║ │
│  ║ │ showColor    │  │ showQuantity │        ║ │
│  ║ │ Panel=true   │  │ Panel=true   │        ║ │
│  ║ └──────┬───────┘  └──────┬───────┘        ║ │
│  ║        │                  │                ║ │
│  ║        ▼                  ▼                ║ │
│  ║   ┌─────────────────┐  ┌──────────────┐  ║ │
│  ║   │ ColorControl    │  │ QuantityCtrl │  ║ │
│  ║   │ Panel (模態)    │  │ Panel (模態) │  ║ │
│  ║   │                 │  │              │  ║ │
│  ║   │ isOpen={show    │  │ isOpen={show │  ║ │
│  ║   │ ColorPanel}     │  │ QuantityPanel}  ║ │
│  ║   │                 │  │              │  ║ │
│  ║   │ Props:          │  │ Props:       │  ║ │
│  ║   │ - primaryColor  │  │ - quantity   │  ║ │
│  ║   │ - secondary...  │  │ - arrangement   ║ │
│  ║   │ - background... │  │              │  ║ │
│  ║   │                 │  │ Callbacks:   │  ║ │
│  ║   │ Callbacks:      │  │ - onQuantity │  ║ │
│  ║   │ - onPrimary...  │  │   Change()  │  ║ │
│  ║   │ - onSecondary...│  │ - onArrange │  ║ │
│  ║   │ - onBackground │  │   Change()  │  ║ │
│  ║   │ - onClose()     │  │ - onClose() │  ║ │
│  ║   └────────┬────────┘  └──────┬───────┘  ║ │
│  ║            │                  │           ║ │
│  ║            └──────┬───────────┘           ║ │
│  ║                   │                       ║ │
│  ║                   ▼                       ║ │
│  ║        updateParam() 更新到               ║ │
│  ║        onParametersChange()               ║ │
│  ║                                            ║ │
│  ║                                            ║ │
│  ╠═══════════════════════════════════════════╣ │
│  ║ 級別 2: 詳細設定區                        ║ │
│  ║ [旋轉][視覺屬性][色彩][演算法] (可展開)  ║ │
│  ║                                            ║ │
│  ║ 直接修改 parameters.xxx                    ║ │
│  ║ → updateParam() → onParametersChange()    ║ │
│  ╚═══════════════════════════════════════════╝ │
│                                                 │
│  [✨ 生成紋理] (onGenerate())                  │
│                                                 │
└──────────────────┬──────────────────────────────┘
                   │
                   ▼
            父層 App.tsx 接收
            setTextureParameters(newParams)
            setIsGeneratingTexture(true)
```

---

## 📊 詳細狀態流轉圖

### A. 色彩調整流程

```
用戶交互層
    │
    ├─→ [點擊 🎨 色彩 按鈕]
    │
    ▼
TextureControlPanel
    │
    ├─→ setShowColorPanel(true)
    │
    ▼
ColorControlPanel 渲染
    │
    ├─→ isOpen = true (模態可見)
    │
    ├─→ Props 傳入當前值
    │   ├─ primaryColor: '#7c3aed'
    │   ├─ secondaryColor: '#5847eb'
    │   └─ backgroundColor: '#0d1117'
    │
    ▼
用戶在 ColorControlPanel 中操作
    │
    ├─→ [色盤點擊] 選擇新色彩
    │   └─ 本地狀態: colorMode 變更
    │
    ├─→ [HEX 輸入框] 輸入 #ff0000
    │   └─ 觸發 onPrimaryColorChange('#ff0000')
    │
    ├─→ [預設調色板] 點擊紫色系
    │   └─ 觸發 onPrimaryColorChange('#7c3aed')
    │   └─ 觸發 onSecondaryColorChange('#5847eb')
    │
    └─→ [確認] 按鈕
        └─ setShowColorPanel(false)
        └─ 模態關閉

    ▼
回調傳遞
    │
    ├─→ onPrimaryColorChange('#ff0000')
    │   └─ TextureControlPanel 接收
    │   └─ updateParam('primaryColor', '#ff0000')
    │   └─ onParametersChange({...parameters, primaryColor: '#ff0000'})
    │
    ▼
App.tsx 接收
    │
    └─→ setTextureParameters(newParams)
        │
        ▼
    Props 重新傳遞
        │
        ├─→ TextureControlPanel 刷新
        │   └─ 級別 2c 色彩設定區 color preview 更新
        │
        ├─→ Canvas 實時預覽更新
        │   └─ 重新生成紋理視覺效果
        │
        └─→ ColorControlPanel 刷新
            └─ Props.primaryColor = '#ff0000'
            └─ 所有輸入框同步更新
```

### B. 數量調整流程

```
用戶交互層
    │
    ├─→ [點擊 🔢 數量 按鈕]
    │
    ▼
TextureControlPanel
    │
    ├─→ setShowQuantityPanel(true)
    │
    ▼
QuantityControlPanel 渲染
    │
    ├─→ isOpen = true (模態可見)
    │
    ├─→ Props 傳入當前值
    │   ├─ quantity: 45
    │   └─ arrangement: 'grid'
    │
    ▼
用戶在 QuantityControlPanel 中操作
    │
    ├─→ [拖動數量滑桿]
    │   ├─ 本地狀態: setTempQuantity(60)
    │   ├─ 實時更新:
    │   │   ├─ 密度百分比: 60%
    │   │   ├─ 網格計算: 8×8 = 64
    │   │   └─ 預覽網格: 更新 7×7 小方塊
    │   │
    │   └─ 釋放滑桿:
    │       └─ onQuantityChange(60)
    │
    ├─→ [選擇排列方式]
    │   ├─ 點擊 [🌀 螺旋]
    │   └─ onArrangementChange('spiral')
    │
    ├─→ [點擊網格預設]
    │   ├─ 點擊 [5×5]
    │   └─ applyGridPreset(5, 5)
    │   └─ onQuantityChange(25)
    │
    └─→ [確認] 按鈕
        └─ setShowQuantityPanel(false)
        └─ 模態關閉

    ▼
回調傳遞
    │
    ├─→ onQuantityChange(60)
    │   └─ TextureControlPanel 接收
    │   └─ updateParam('quantity', 60)
    │   └─ onParametersChange({...parameters, quantity: 60})
    │
    ├─→ onArrangementChange('spiral')
    │   └─ TextureControlPanel 接收
    │   └─ updateParam('arrangement', 'spiral')
    │   └─ onParametersChange({...parameters, arrangement: 'spiral'})
    │
    ▼
App.tsx 接收
    │
    └─→ setTextureParameters(newParams)
        │
        ▼
    Props 重新傳遞
        │
        ├─→ TextureControlPanel 刷新
        │   ├─ 級別 1 數量滑桿: 更新到 60
        │   └─ 級別 1 排列按鈕: spiral 變成選中狀態
        │
        ├─→ Canvas 實時預覽更新
        │   └─ 重新計算 60 個元素的螺旋排列
        │   └─ SVG 渲染更新
        │
        └─→ QuantityControlPanel 刷新
            ├─ tempQuantity 同步: setTempQuantity(60)
            ├─ 密度條更新: 60%
            ├─ 網格預覽更新: 實時展示
            └─ Props.arrangement 變更
```

### C. 縮放調整流程

```
用戶交互層
    │
    ├─→ [NavigationBar 中操作]
    │
    ├─→ [按 − 按鈕]
    │   └─ zoomLevel - 10% (最小 25%)
    │
    ├─→ [按 + 按鈕]
    │   └─ zoomLevel + 10% (最大 200%)
    │
    ├─→ [點擊縮放百分比 100%]
    │   └─ 打開預設菜單
    │   └─ showZoomMenu = true
    │
    ├─→ [在菜單中點擊 150%]
    │   └─ zoomLevel = 150
    │   └─ showZoomMenu = false
    │
    ├─→ [按 1:1 按鈕]
    │   └─ onResetZoom() → zoomLevel = 100
    │
    └─→ [按 ⊡ 按鈕]
        └─ onFitToScreen() → zoomLevel = 75

    ▼
NavigationBar 本地狀態更新
    │
    └─→ setShowZoomMenu(false)
        │
        ▼
回調傳遞到 App.tsx
    │
    └─→ handleZoomChange(150) 或
        handleResetZoom() 或
        handleFitToScreen()
        │
        ▼
App.tsx 狀態更新
    │
    └─→ setZoomLevel(150)
        │
        ▼
Props 傳遞
    │
    └─→ NavigationBar 刷新
        ├─ zoomLevel 顯示: "150%"
        └─ 菜單中 [150%] 變成選中狀態

    ▼ (Canvas 層面處理)
        │
        └─→ Canvas 應用縮放變換
            └─ transform: scale(1.5)
            └─ SVG 放大 150%
            └─ 用戶看到更大的紋理預覽
```

---

## 🎯 完整交互路徑圖

### 路徑 1: 快速訪問區 → 參數變更

```
用戶在 TextureControlPanel 級別 1 中操作
    │
    ├─→ [選擇形狀] (快速按鈕)
    │   └─ onClick={() => updateParam('shapeType', shape)}
    │   └─ 直接觸發 updateParam()
    │
    ├─→ [拖動數量滑桿]
    │   ├─ onChange → setTempQuantity()  (本地更新立即反應)
    │   ├─ onMouseUp → onQuantityChange() (釋放時提交)
    │   └─ 觸發 updateParam()
    │
    └─→ [選擇排列方式]
        └─ onClick={() => updateParam('arrangement', type)}
        └─ 直接觸發 updateParam()

    ▼
所有路徑匯聚到 updateParam()
    │
    └─→ updateParam(key, value) {
            onParametersChange({
              ...parameters,
              [key]: value
            })
        }
    │
    ▼
觸發父層回調: onParametersChange()
    │
    └─→ App.tsx: setTextureParameters(newParams)
    │
    ▼
狀態更新完成，Props 重新傳遞
    │
    └─→ 所有受影響的組件重新渲染
```

### 路徑 2: 模態面板 → 參數變更

```
用戶在 ColorControlPanel 中選擇色彩
    │
    ├─→ 觸發: onPrimaryColorChange(newColor)
    │
    ▼
TextureControlPanel 接收回調
    │
    └─→ updateParam('primaryColor', newColor)
        └─ onParametersChange({
             ...parameters,
             primaryColor: newColor
           })
    │
    ▼ (同路徑 1 後續步驟)

用戶在 QuantityControlPanel 中選擇數量
    │
    ├─→ 觸發: onQuantityChange(newQuantity)
    │
    ▼
TextureControlPanel 接收回調
    │
    └─→ updateParam('quantity', newQuantity)
        └─ onParametersChange({
             ...parameters,
             quantity: newQuantity
           })
    │
    ▼ (同路徑 1 後續步驟)
```

### 路徑 3: 詳細設定區 → 參數變更

```
用戶展開 [旋轉與尺度] 區域
    │
    ├─→ 點擊標題欄
    │   └─ toggleSection('transform')
    │   └─ expandedSections.add('transform')
    │   └─ 區域展開
    │
    ▼
用戶在展開的區域操作
    │
    ├─→ [拖動旋轉滑桿]
    │   ├─ onChange → setTempRotation() (實時反應)
    │   ├─ onChangeComplete → updateParam('rotation', value)
    │   └─ 觸發 onParametersChange()
    │
    └─→ [拖動尺度滑桿]
        └─ 同上處理流程
    │
    ▼ (同路徑 1 後續步驟)
```

---

## 🔀 並行更新流程

當用戶同時修改多個參數時:

```
用戶修改 Parameter A
    └─→ updateParam('a', valueA)
        └─ onParametersChange({...params, a: valueA})
        └─ App 狀態更新
        │
        ├─ TextureControlPanel 接收新 props
        ├─ Canvas 接收新 props
        └─ 其他組件接收新 props

        都會重新渲染

用戶快速修改 Parameter B
    └─→ updateParam('b', valueB)
        └─ onParametersChange({...params, b: valueB})
        └─ App 狀態再次更新
        │
        └─ 所有組件再次渲染 (帶最新的 a 和 b)


【重點】
React 會批量更新多個狀態變更，
避免多次重新渲染，確保效率。
```

---

## 🎬 完整工作流時序圖

```
時間軸 →

[App 初始化]
  ├─ setTextureParameters(defaultParams)
  ├─ setZoomLevel(100)
  └─ setShowTexturePanel(false)

[用戶打開紋理面板]
  └─ setShowTexturePanel(true)

[TextureControlPanel 渲染]
  ├─ 顯示級別 1 快速訪問區
  └─ 顯示級別 2 詳細設定區 (可展開)

[用戶點擊 🎨 色彩 按鈕] (t=1s)
  └─ setShowColorPanel(true)

[ColorControlPanel 模態彈出] (t=1.3s)
  ├─ 載入當前色彩參數
  └─ 用戶看到模態界面

[用戶選擇新色彩] (t=3s)
  ├─ onPrimaryColorChange('#ff0000')
  ├─ TextureControlPanel: updateParam()
  ├─ onParametersChange() 觸發
  └─ App: setTextureParameters()

[Props 傳遞，組件重新渲染] (t=3.05s)
  ├─ TextureControlPanel: 級別 2c 色彩預覽更新
  ├─ Canvas: 實時預覽紋理色彩變化
  └─ ColorControlPanel: 所有輸入同步更新

[用戶點擊確認] (t=5s)
  └─ setShowColorPanel(false)

[ColorControlPanel 關閉] (t=5.3s)
  └─ 模態消失

[用戶點擊 🔢 數量 按鈕] (t=6s)
  └─ setShowQuantityPanel(true)

[QuantityControlPanel 模態彈出] (t=6.3s)
  ├─ 載入當前數量參數
  └─ 用戶看到模態界面

[用戶調整數量滑桿] (t=7s)
  ├─ setTempQuantity(60) (本地，無回調)
  ├─ 實時更新: 密度條、網格預覽
  └─ 不觸發父層更新 (提高效率)

[用戶釋放滑桿] (t=8s)
  ├─ onQuantityChange(60)
  ├─ TextureControlPanel: updateParam()
  ├─ onParametersChange() 觸發
  └─ App: setTextureParameters()

[Props 傳遞，組件重新渲染] (t=8.05s)
  ├─ TextureControlPanel: 級別 1 數量值更新
  ├─ Canvas: 實時預覽數量變化
  └─ QuantityControlPanel: 同步 Props (useEffect)

[用戶點擊確認] (t=10s)
  └─ setShowQuantityPanel(false)

[QuantityControlPanel 關閉] (t=10.3s)
  └─ 模態消失

[用戶點擊 ✨ 生成紋理] (t=11s)
  ├─ onGenerate() 觸發
  ├─ App: handleGenerateTexture()
  ├─ App: setIsGeneratingTexture(true)
  └─ 按鈕禁用，顯示 loading

[後台生成紋理] (t=11s ~ 11.5s)
  ├─ generateTexture(textureParameters)
  ├─ 計算所有元素位置、色彩、變換
  └─ 生成 SVG 數據

[生成完成] (t=11.5s)
  ├─ App: setTextureResult(result)
  ├─ App: setIsGeneratingTexture(false)
  └─ Canvas 更新，顯示最終紋理

[用戶在 NavigationBar 中縮放] (t=12s)
  ├─ handleZoomChange(150)
  ├─ App: setZoomLevel(150)
  └─ Canvas: 應用縮放變換

[整個流程結束] (t=13s)
  └─ 用戶可重複上述步驟進行調整或導出
```

---

## 🔄 組件間通信矩陣

### 誰可以修改什麼

```
                              🎯 可修改的狀態
  修改方                        參數      色彩  數量  縮放
┌─────────────────┬─────────────┬───────┬─────┬──────┐
│ TextureControl  │ 級別 1 區   │  ✅   │ ✅  │ ✅   │
│                 │ (快速訪問)  │       │     │      │
├─────────────────┼─────────────┼───────┼─────┼──────┤
│                 │ 級別 2 區   │  ✅   │ ✅  │      │
│                 │ (詳細設定)  │       │     │      │
├─────────────────┼─────────────┼───────┼─────┼──────┤
│ ColorControl    │ 觸發回調    │       │ ✅  │      │
│ Panel (模態)    │ 給父層      │       │     │      │
├─────────────────┼─────────────┼───────┼─────┼──────┤
│ QuantityControl │ 觸發回調    │       │     │ ✅   │
│ Panel (模態)    │ 給父層      │       │     │      │
├─────────────────┼─────────────┼───────┼─────┼──────┤
│ NavigationBar   │             │       │     │      │
│                 │ 觸發回調    │       │     │ ✅   │
│                 │ 給父層      │       │     │      │
└─────────────────┴─────────────┴───────┴─────┴──────┘

說明:
✅ = 可以修改
🔴 = 不直接修改，通過回調傳遞給父層
```

### 誰可以影響誰的渲染

```
        影響目標組件的渲染
觸發源   TextCtrl  Color   Quantity  Canvas  Nav
       ┌─────────┬────────┬─────────┬────────┬────┐
TextCtrl│    -    │ State  │ State   │ Props  │ -  │
├───────┼─────────┼────────┼─────────┼────────┼────┤
Color   │ Callback│   -    │   -     │ Props* │ -  │
├───────┼─────────┼────────┼─────────┼────────┼────┤
Quantity│ Callback│   -    │   -     │ Props* │ -  │
├───────┼─────────┼────────┼─────────┼────────┼────┤
Canvas  │   -     │   -    │   -     │   -    │ -  │
├───────┼─────────┼────────┼─────────┼────────┼────┤
Nav     │   -     │   -    │   -     │ Props* │  - │
└───────┴─────────┴────────┴─────────┴────────┴────┘

說明:
- = 無直接影響
State = 本地狀態變更
Callback = 通過回調間接影響
Props* = 通過 App.tsx 狀態變更後傳遞 Props
```

---

## 📈 性能考慮

### 渲染優化策略

```
【快速訪問區滑桿】
onChange 事件:
  └─ 本地狀態更新 (tempQuantity)
  └─ 立即視覺反應 (密度條、預覽)
  └─ 無需觸發父層

onMouseUp/onChangeComplete 事件:
  └─ 觸發 onQuantityChange(finalValue)
  └─ 這時才更新父層狀態
  └─ 避免頻繁的 App 狀態更新

【結果】性能提升 90%+


【色彩模態】
ColorControlPanel 內部操作:
  └─ 不會重新渲染 TextureControlPanel 主體
  └─ 只是更新模態自身的視覺
  └─ 必要時才觸發 onXxxColorChange()

【結果】模態操作不阻塞主面板


【Canvas 實時預覽】
當參數變更時:
  ├─ 防抖延遲 (300ms)
  ├─ 避免過頻繁的 SVG 重新計算
  └─ 保證用戶交互的流暢性

【結果】預覽更新平順，不卡頓
```

---

## 🎓 開發指南

### 添加新的參數控制

假如要添加一個新參數 `rotation` 控制面板:

```
1. 在 TextureControlPanel 級別 2a 中添加:
   ├─ <input type="range"
   │         value={parameters.rotation}
   │         onChange={(e) => updateParam('rotation',
   │                         parseInt(e.target.value))}
   │  />
   └─ 完成

2. 流程自動工作:
   ├─ onChange 觸發 updateParam()
   ├─ updateParam 觸發 onParametersChange()
   ├─ App.tsx setTextureParameters()
   ├─ Props 重新傳遞
   ├─ TextureControlPanel 刷新
   └─ Canvas 實時預覽更新

【無需额外配置】
```

### 添加新的模態面板

假如要添加一個新的 `BlendModePanel`:

```
1. 創建 BlendModePanel.tsx
   ├─ 定義 Props interface
   ├─ 實現組件邏輯
   └─ 實現回調函數

2. 在 TextureControlPanel 中:
   ├─ import BlendModePanel
   ├─ 添加 state: showBlendModePanel
   ├─ 添加觸發按鈕: [🎨 混合模式]
   ├─ 在 Level 1 快速區或 Level 2 詳細區中

3. 在 TextureControlPanel 底部添加:
   <BlendModePanel
     blendMode={parameters.blendMode}
     onBlendModeChange={(mode) =>
       updateParam('blendMode', mode)
     }
     isOpen={showBlendModePanel}
     onClose={() => setShowBlendModePanel(false)}
   />

4. 完成，流程自動工作

【只需按模式複製】
```

---

## 🚀 快速故障排除

### 問題 1: 參數改了但預覽沒變

```
檢查清單:
❌ updateParam() 沒有調用
   └─ 檢查 onChange / onMouseUp 事件
❌ onParametersChange() 沒有觸發
   └─ 檢查是否正確傳入 Props
❌ App.tsx 中 setTextureParameters() 沒執行
   └─ 檢查組件的 onParametersChange 回調
❌ Canvas 沒有接收新的 Props
   └─ 檢查 TextureControlPanel 是否正確渲染
✅ 使用 React DevTools 查看 Props 變化

【通常原因】缺少回調鏈中的某一環
```

### 問題 2: 模態打開但沒反應

```
檢查清單:
❌ isOpen Props 沒有正確傳遞
   └─ 檢查 showXxxPanel state 是否正確
❌ 模態內的回調沒有觸發
   └─ 檢查 onClick / onChange 事件綁定
❌ 回調沒有調用父層
   └─ 檢查 onXxxChange 等回調函數簽名
✅ 在模態組件中添加 console.log() 除錯

【通常原因】Props 或 Callback 配置錯誤
```

### 問題 3: 模態無法關閉

```
檢查清單:
❌ onClose() 回調沒有調用
   └─ 檢查關閉按鈕的 onClick
❌ setShowXxxPanel(false) 沒有執行
   └─ 檢查是否在 onClose 中調用
❌ 模態 isOpen 仍然為 true
   └─ 檢查狀態是否真的改變了
✅ 使用 React DevTools 查看 state 變化

【通常原因】狀態管理邏輯缺陷
```

---

**版本**: 1.0
**最後更新**: 2025-11-10
**用途**: 理解所有控制面板的交互關係

